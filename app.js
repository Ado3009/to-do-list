
/*
function addList(){

let unesi_tekst = document.querySelector('#inputLabel')
let kontenjer = document.querySelector('.container')
let button = document.querySelector('#btn')

novi_el.innerText = unesi_tekst.value

unesi_tekst.value = '';


}
 

    let novi_el = document.createElement('p')
    novi_el.classList = ('novi')
    document.querySelector('.container').appendChild(novi_el)

    */
/*
// selektovanje dijelova iz html 
    let unesi_tekst = document.querySelector('.inputLabel')
    let kontenjer = document.querySelector('.container')
    let button = document.querySelector('#btn')

// dodavanje click funkcije za button kako bi izbacio ono sto upisemo    
    button.addEventListener('click', function(e) {
        let novi_el = document.createElement('p')
        novi_el.innerText = unesi_tekst.value
        kontenjer.appendChild(novi_el)
        novi_el.classList.add('novi')
         
        unesi_tekst.value = '';

        novi_el.addEventListener('click', function(e){
            novi_el.style.textDecoration = ("line-through")

        })
        
        novi_el.addEventListener('dblclick', function(e){
            kontenjer.removeChild(novi_el)

        })
        
    })
*/